import styled from "styled-components"

const Card = styled.section`
  border-radius: 4px;
  min-width: 19em;
  max-width: 60em;
  display: flex;
  margin-bottom: 0.7em;
  flex-direction: column;
  overflow: hidden;
`;

export default Card